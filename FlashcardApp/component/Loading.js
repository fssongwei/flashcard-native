import React from "react";
import { Text } from "react-native";
import Screen from "./Screen";

const Loading = () => {
  return (
    <Screen>
      <Text>Loading</Text>
    </Screen>
  );
};

export default Loading;
